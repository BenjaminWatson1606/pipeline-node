{
  "name": "pipeline-node",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage --coverageReporters=text --coverageReporters=lcov && cat ./coverage/lcov-report/index.html > coverage.txt"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@commitlint/cli": "^19.4.0",
    "@commitlint/config-conventional": "^19.2.2",
    "jest": "^29.0.0"
  },
  "jest": {
    "testMatch": ["**/__tests__/**/*.test.js?(x)"]
  }
}
